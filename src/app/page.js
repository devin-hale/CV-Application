import PageHeader from "@/components/PageHeader.js";
import GeneralInfo from "@/components/generalInfo";

const Home = () => {
    return (
        <div>
            <PageHeader />
            <GeneralInfo />
        </div>
    );
};

export default Home;
